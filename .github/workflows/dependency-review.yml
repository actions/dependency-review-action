name: 'Dependency Review'

on: [pull_request]

permissions:
  contents: read

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout Repository'
        # TODO(security): Pin external actions to full-length commit SHAs after owner/update process review.
        uses: actions/checkout@v6
      - name: Dependency Review
        uses: ./
